To make running your model code with it’s package and library dependencies as easy as possible we want you to write a Dockerfile that we can use to exactly reproduce the environment you used to make your submission.

